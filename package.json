{
  "name": "@qualibot/github-action",
  "version": "1.0.0",
  "private": true,
  "description": "QualiBot GitHub Action - Automated visual testing for pull requests",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "build:watch": "ncc build src/index.ts -o dist --source-map --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0"
  }
}